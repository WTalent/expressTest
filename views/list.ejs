<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>文章列表</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" >
   
    <style type="text/css">
       .update{
           left:50%;
           transform:translate(-50%,0);
       }
    </style>
</head>
<body>
    <!--导航条-->
               <%- include('header.ejs') %>

    <!--文章列表-->    
    <% for(let i=0;i<list.length;i++) {%>
    <div class="card my-5">
        <div class="card-body">
          <h5 class="card-title"><a href="/list/<%= list[i]._id %>"><%=list[i].title %></a></h5>
          <p class="card-text"><%=list[i].content %></p>
          <p class="card-text"><%=list[i].updatedAt %></p>
        </div>
    </div> 
      <% } %>

      <!--分页-->
      <nav class="my-5">
        <ul class="pagination position-absolute update">
          <li class="page-item">
            <a class="page-link" href="/list?pageNum=<%= pageNum===1?1:pageNum-1%>" aria-label="Previous">
              <span aria-hidden="true">&laquo;</span>
              <span class="sr-only">Previous</span>
            </a>
          </li>
          <% for(let i=1;i<=total;i++){ %>
          <li class="page-item <%= pageNum===i?'active':'' %>"><a class="page-link" href="/list?pageNum=<%=i%>"><%= i %></a></li>
          <% } %>
          <li class="page-item">
            <a class="page-link" href="/list?pageNum=<%= pageNum===total?total:pageNum+1 %>" aria-label="Next">
              <span aria-hidden="true">&raquo;</span>
              <span class="sr-only">Next</span>
            </a>
          </li>
        </ul>
      </nav>
<%- include("footer.ejs") %>